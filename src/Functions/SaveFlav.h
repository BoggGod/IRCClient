#ifndef _SaveFlav_H
#define _SaveFlav_H

#include <map>
#include <string>
#include <fstream>
#include <cstdio>
#include <algorithm>
#include <iostream>
#include <vector>

void SaveFlav(std::map<std::string, std::vector<std::string>> &themap, std::string &fname);

#endif
