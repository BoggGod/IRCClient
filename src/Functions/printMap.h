#ifndef _printMap_H
#define _printMap_H

#include <string>
#include <map>
#include <fstream>
#include <iostream>
#include <cstdio>

void printMap(std::map<std::string, std::string> &themap, std::string &fname);

#endif
