#ifndef _general_H
#define _general_H

#include <algorithm> 
#include <cctype>
#include <locale>
#include <string>

void ltrim(std::string &s);
void eraseSubstr(std::string &str, const std::string &toErase);

#endif
